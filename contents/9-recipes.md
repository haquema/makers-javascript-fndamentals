# Thermostat: fetching current weather

## Current weather

* Test-drive and implement a new class `Weather`. This class should have one method `getCurrentWeather` that accepts a city name and retrieves the current weather.